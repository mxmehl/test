name: REUSE Compliance Check

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pwd
    - run: ls -la
    - run: git status
    - run: git config -l
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@a959c6a62b48f8fbde9296862ed468df19368865
