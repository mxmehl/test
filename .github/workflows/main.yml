name: REUSE Compliance Check

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pwd
    - run: ls -la
    - run: git status
    - run: git config -l
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@90405654c457eea143d61c7cfb7a0efa513240d3
