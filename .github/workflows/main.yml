name: REUSE Compliance Check

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pwd
    - run: ls -la
    - run: git status
    - run: git config -l
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@0df56e3504c58e77ee7f227898172feef62f54bc
