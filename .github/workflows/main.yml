name: REUSE Compliance Check

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pwd
    - run: ls -la
    - run: git status
    - run: git config -l
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@24008bdce6cf07292001511adfb5f8728afa8a59
